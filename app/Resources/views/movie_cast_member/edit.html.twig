{% extends 'base.html.twig' %}
{% block sidebar %}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Edit Movie Cast Member</h1>
                {{ form_start(castMemberForm) }}
                {{ form_row(castMemberForm.person) }}
                {{ form_row(castMemberForm.role) }}
                <button type="submit" class="btn btn-primary" formnovalidate>Save</button>
                {{ form_end(castMemberForm) }}
            </div>
        </div>
    </div>
{% endblock %}