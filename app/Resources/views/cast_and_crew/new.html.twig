{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Movie Cast And Crew</h1>
                {{ form_start(castAndCrewForm) }}
                {{ form_widget(castAndCrewForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                <a class="btn btn-xs btn-success pull-right" href="{{ path('new_person') }}">+ CreateNew Person</a>
                {{ form_end(castAndCrewForm) }}

            </div>
        </div>
    </div>
{% endblock %}